#include "monty.h"
/**
 * f_stack - the print the top
 * @head: the stack head
 * @counter: the line_number
 * Return: return null
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
